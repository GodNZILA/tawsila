<div class="container">
  <h2>Liste de vos voitures</h2>
    <div class="listes-voitures">
      <% @voitures.each do |voiture| %>
        <ul>
          <%= voiture.marque %>
          <%= voiture.model %>
          <%= voiture.immatriculation %>
        </ul>
      <% end %>
    </div>

</div>
