<h2>Liste bookings</h2>
<div class="card_booking">
  <% @bookings.each do |booking|%>
    <div class="card_booking">
      <p><%= booking.prix %></p>
      <p><%= booking.statut %></p>
    </div>
  <% end %>
</div>
