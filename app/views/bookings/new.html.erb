<div class="new-booking">

<div class="title p-3">
    <div class="d-flex justify-content-between align-items-center mb-3" >
  <%= image_tag "logo", style: "height: 40px" %>
        <h2>Réserver</h2>
  <%= image_tag "avatar", style: "height: 40px; border-radius: 50%" %>
  </div>

<br>
<div class="form_new">
  <%= simple_form_for [@offre, @booking] do |f|  %>
    <%= f.input :depart %>
    <%= f.input :arrivee %>
    <%= f.submit "Valider" %>
  <% end %>
</div>

  <% image_id = @offre.voiture_id %>
  <% key = Voiture.find(image_id).photo.key %>
  <br>
  <div class= "image_class">
  <%= cl_image_tag key, height: 200, width: 300, crop: :fill  %>
  </div>
<%  %>
</div>
