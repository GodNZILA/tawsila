<div class="title p-3">
    <div class="d-flex justify-content-between align-items-center mb-3" >
  <%= image_tag "logo", style: "height: 40px" %>
        <h2>Proposer un trajet</h2>
  <%= image_tag "avatar", style: "height: 40px; border-radius: 50%" %>
  </div>
  </div>
<br>
<br>
<br>
<br>
<h2 class="p-3"><%= @offre.description_trajet %></h2>
  <div class="card-offre m-3 p-3">

 <div class= "d-flex gap-2 align-items-center">
        <%= image_tag "location" %>
        <span><%= @offre.depart %></span>
      </div>
    <%= image_tag "trajet" %>
      <div class= "d-flex gap-2 align-items-center">
        <%= image_tag "location" %>
        <span><%= @offre.arrivee %></span>
      </div>
      <div class= "d-flex gap-2 align-items-center mt-2">
        <%= image_tag "date" %>
        <span><%= @offre.heure_depart %></span>
      </div>
      <div class= "d-flex gap-2 align-items-center mt-2">
        <%= image_tag "price" %>
        <span><%= @offre.prix %> MAD</span>
      </div>
</div>

      <%= link_to "Archiver l'offre", offre_path(@offre), class: "btn btn-danger-sm m-3", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} if @is_creator%>

<% unless @is_creator %>
  <div class="book-card p-3">
    <%= simple_form_for @booking do |f|  %>
      <%= f.input :depart %>
      <%= f.input :arrivee %>
      <%= f.input :offre_id, as: :hidden %>

      <%= f.submit "Réserver le trajet", class: "btn btn-primary-sm" %>
    <% end %>
  </div>
<% end %>
